# Claude Code Instructions - Aveil Health

If `../AGENTS.md` exists, read it for Aveil multi-repo workflow, then read this
repo's `AGENTS.md` for CLI/MCP-specific instructions.

The parent guide owns shared Aveil positioning, branch rules, and cross-repo
contracts. This repo guide owns local Apple Health export analysis, CLI/MCP files,
privacy boundaries, and verification gates.

The canonical instructions are in `AGENTS.md` files so Claude Code and Codex use
the same source. This file exists because Claude Code reads `CLAUDE.md` by
default.
